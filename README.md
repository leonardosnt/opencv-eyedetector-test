# opencv-eyedetector-test

### Iniciar
```
mvn install
mvn exec:java
```

Obs: O arquivo opencv_java320.dll é para 64 bits, caso você esteja rodando 32 bits você precisará baixar o OpenCV e alterar pelo de 32 bits.